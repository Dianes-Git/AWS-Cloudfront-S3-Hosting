variable "bucket_name" {
  default = "<YOUR_UNIQUE_BUCKET_NAME>" #Replace <YOUR_UNIQUE_BUCKET_NAME> with your actual S3 bucket name. Ensure it is globally unique, as S3 bucket names must be unique across all AWS accounts
}

